import Foundation

enum ProductIdentifiers {
    /// Single one-time purchase — unlocks everything forever
    static let fullVersion = "com.judgymac.full"

    static let all: Set<String> = [fullVersion]
}
